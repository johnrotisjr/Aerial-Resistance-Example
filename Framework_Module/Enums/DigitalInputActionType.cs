namespace Framework_Module.Enums
{
    /// <summary>
    /// Enumerates the types of digital input actions (e.g., FireGun, Pause).
    /// Used to map input actions to specific command objects.
    /// </summary>

    public enum DigitalInputActionType
    {
        Submit,
        Cancel,
        FireGun,
        Pause,
        FireWeapon,
        ChangeWeapon,
        Debug
    }
}
