namespace Framework_Module.Enums
{
    /// <summary>
    /// Enumerates the types of axis input actions (e.g., Move).
    /// Used to map vector-based input to movement or aiming logic.
    /// </summary>

    public enum AxisInputActionType
    {
        Move,
        Navigate,
    }
}
