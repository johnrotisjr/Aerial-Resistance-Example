namespace Debug_Module
{
    /// <summary>
    /// Defines severity levels for logs (Verbose, Log, Warning, Error).
    /// Used to filter and prioritize debug output.
    /// </summary>

    public enum LogLevel
    {
        Log,
        Warning,
        Error,
    }
}